# Ludoria web app

The Next.js page hosts the JavaScript game in `public/game/`. See the repository README for the dependency-free game server and static build, or run `npm run dev --workspace web` from the repository root for the Next.js wrapper.
